<p id="notice"><%= notice %></p>

<div class="page-header">
  <h1>Venues</h1>
</div>

<%= link_to t('.back', :default => t("helpers.links.back")),
              root_path, :class => 'btn btn-default'  %>
<br>
<br>

<% @venues.each do |venue| %>
  <%= link_to venue, menus_by_venue_menus_path(venue: venue) %>
  <br>
<% end %>

<br>
<%= link_to t('.back', :default => t("helpers.links.back")),
              root_path, :class => 'btn btn-default'  %>
<br>
<br>
